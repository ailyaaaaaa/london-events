<!doctype html>
<html>
  <head>
    <title>Welcome to <%=data.name%></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <%- include('header') %>
  <body>
    <h1><%=data.name%></h1>
    <h2>All users:</h2>
    <ul>
      <% availableUsers.forEach(function(users) { %>
        <li><%= users.firstname%>, <%=users.lastname%>, <%=users.username%> </li>
      <% }) %>
    </ul>
    </body>
</html>
